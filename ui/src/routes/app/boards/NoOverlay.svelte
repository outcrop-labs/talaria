<script lang="ts">
  // /boards/:boardId with no ticket open: BoardLayout renders the board; this
  // child intentionally renders nothing. The empty TS block keeps the module
  // typed for router.ts's eager-typed import (same trick as NotFound.svelte).
</script>
