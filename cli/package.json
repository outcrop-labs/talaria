{
  "name": "talaria-cli",
  "version": "0.1.0",
  "private": true,
  "description": "The `talaria` command — everything the bash scripts in scripts/ used to do, typed and testable. Run from the repo root: `bun talaria …`.",
  "license": "MIT",
  "type": "module",
  "bin": { "talaria": "bin/talaria.ts" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "@types/bun": "^1.2.0",
    "@types/node": "^22.15.0"
  }
}
