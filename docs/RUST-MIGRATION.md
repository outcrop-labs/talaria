# The Rust port

The API is moving from TypeScript to Rust: `ui/src/server` + `ui/src/routes/api`
(~80k LOC, 219 routes) port in dependency order to the `api/` crate (axum, sqlx,
redis-rs, hand-rolled reqwest clients), until the TS API is deleted. The scope is
the BACKEND and nothing else — the SvelteKit server keeps serving the SPA, its
pages and their routing exactly as it does today; the port ends with Rust owning
every `/api/*` route, SSE and the scheduler, not with a rewrite of the product
and not with Rust hosting the UI.

This is a **wholesale port, not a strangler-fig forever**: nothing is live and
there are no clients, which is the one window where the whole backend can change
shape. The coexistence proxy below exists so the app keeps working while the port
grinds — at cutover the TS API route table behind it is deleted, but the hop
itself is the end-state architecture, not scaffolding: the SvelteKit server stays
the one public origin and hands `/api/*` to Rust on loopback, which is what
keeps the cookies same-origin (rule 7).

When this page and the source disagree, the source wins.

## The rules of the port

These hold for every batch. Each one exists because breaking it breaks both
runtimes at once, not just the one being edited.

1. **TS owns the schema until batch 4.** The `MIGRATIONS` array in
   `ui/src/server/db/pg.ts` is append-only with sha256 checksums under an
   advisory lock; the Rust side **never migrates, never emits DDL** — sqlx reads
   and writes existing tables only, with runtime queries (no `query!` macros:
   the schema moves with the TS array, and compile-time checking against a
   per-devbox DB would be brittle). Batch 4 hands schema ownership to sqlx; until
   then a new column means a TS migration first, a Rust reader second.
2. **One route group, one runtime.** A migrated prefix serves from Rust or from
   TS, never both. The proxy has **no fallback on purpose**: if the Rust api is
   down, a migrated prefix answers 502 `upstream unreachable`, because silent
   fallback to the TS path is exactly the both-runtimes-serve-one-group failure
   mode the design refuses.
3. **Byte-parity with TS is the bar while both exist.** The UI is the client —
   shapes stay faithful because the SPA consumes them, not because the shapes are
   sacred. Verified by diffing responses from `:5273` (TS) against `:5274`
   (Rust) on the same dev DB before a prefix flips. serde structs are declared
   in wire order (serde_json `preserve_order` on round-trips) — `JSON.stringify`
   key order is observable behavior. Recorded divergences live at the bottom of
   this page.
4. **secretbox speaks both languages.** `api/src/secretbox.rs` is byte-compatible
   with `ui/src/server/secretbox.ts` (same KEK derivation, same token grammar),
   pinned by one committed fixture asserted from both sides
   (`api/tests/secretbox.rs`, `ui/src/server/secretbox.fixtures.test.ts`).
   Regenerate with `bun run api:vectors`; `api:check` fails on a stale fixture.
5. **The scheduler runs in exactly one runtime.** `TALARIA_SCHEDULER` is the
   whole switch, read by both sides: unset = TS arms (every existing
   deployment), `rust` = the Rust api registers and arms the table while TS's
   `startScheduler` stands down, `off` = nobody arms. One value in one env
   file, so there is no window where both runtimes think a period is theirs —
   and the shared `sched` lease namespace backstops a botched value besides.
   The Rust arm (`api/src/jobs.rs`) refuses to arm until the census's six run
   kinds all have definitions (its boot log names the missing ones), because
   `run-reclaim` strands rows of kinds it cannot define once TS's sweep is
   down. Until a deployment sets `rust`, Rust arms nothing and TS's schedule
   is untouched.
6. **`mcp/` stays TS**, and `GET /api/users` remains the fleet-wide auth oracle
   with its shape frozen — the MCP server authenticates every agent through it
   (`agent-auth.ts`), so the port must not move it until the session plane
   (batch 2) is served from Rust and proven.
7. **Same origin, no CORS.** Cookies are HttpOnly SameSite=Lax against one
   origin; the proxy is the only thing that ever hops between runtimes, on
   loopback, stripping hop-by-hop headers.
8. **Upstream error text dies at the boundary.** The gateway relays status codes
   and fixed sentences, never a provider's prose — the TS rule
   (`server/upstream-error.ts`), ported and byte-tested in `api/src/error.rs`.
9. **The unit suites are pure.** No test in `api/` or `ui/` needs a service in
   CI; the `#[ignore]`d integration tests that do touch the dev DB run locally
   with `cargo test -- --ignored`, never in CI. `bun run verify` never scans
   `api/`; the crate's gates are `bun run api:check` (fmt + clippy `-D warnings`
   + test) and the `api` CI job.
10. **App modules are customer code, not port surface.** Building a microapp
   with the SDK stays a TS/node experience: an app's internal APIs are the
   app author's own code, talking to the host through the same `/api` and UI
   surfaces everyone else uses, and the port never reaches into
   `apps/<slug>/` to rewrite that. The app-server gateway (`/api/apps`,
   `apps.$app.$` dispatch) may cross to Rust as HOST plumbing, but the
   modules it dispatches into keep their TS/node runtime forever — that is
   the accessibility promise, not a migration debt. Rust API modules are the
   advanced tier: a self-contained option an app author can opt into, which
   coexists with (never replaces) the TS default. Rule 2's
   one-runtime-per-group governs the host's routes, never app-owned code.

## How coexistence works

`ui/src/server/rust-proxy.ts` is the switch. A compiled `PREFIXES` list, checked
at the top of `app.ts`'s handler before the route table: a request under a
migrated prefix is forwarded to `TALARIA_RUST_API_URL` (loopback `:5274` in dev),
response bodies streamed through. Unset env — the default — forwards nothing:
every route is served by TS, byte-identical to before the port existed. An
`EXACT` list sits beside the prefixes for whole-path migrations — a route whose
sub-paths still belong to TS (`/api/agents` next to `register`/`heartbeat`,
`/api/apps` next to the app-server gateway) migrates by exact pathname, or the
prefix would strand those sub-routes on a Rust 404.

Flipping a group is two edits: the prefix joins `PREFIXES`, and `ui/.env` sets
`TALARIA_RUST_API_URL` (see `ui/.env.example` for the block). In dev,
`TALARIA_API=on` makes `talaria dev` start the Rust api as a sidecar (`cargo
run` in `api/`, adopting an instance already on the port, signals forwarded,
readiness polled without blocking the app); a devbox carries the whole toolchain
(`docker/devbox.Dockerfile` — pinned 1.97.1, mirroring `api/rust-toolchain.toml`).

## The batches

Dependency-ordered; each batch's prefixes flip only when its exit criteria hold
against the live dev stack.

| # | Scope | Exit criteria | Status |
|---|---|---|---|
| 1 | **LLM gateway** — `/api/llm/v1/*`: models, chat/completions (streaming + not), `tlk_` key auth with per-key caps and throttling, rate limits, the usage ledger, secretbox unseal, the confab guard (off/observe/annotate/strict, streaming caveat chunk, strict redaction) | byte-diff vs TS on models; live streams through the real provider hop with ledger rows either end of the stream; guard modes verified row-by-row in `guard_findings`; key caps enforced | **served from Rust** |
| 2 | **Sessions and auth** — Redis `sess:<sid>` sessions, Google OAuth (hand-rolled, no SDK), password credentials, invites, the users routes (oracle shape frozen until this lands) | a browser session held across a runtime hop; OAuth round-trip; the SPA fully usable with the session plane on Rust | **served from Rust** — the session store, `/api/auth/session`, `/api/auth/logout`, `/api/users` (oracle), `/api/auth/password`, `/api/auth/providers`, `/api/auth/claim`, `/api/auth/google` + `/callback`, all byte-diffed against TS on the same sessions and the same login counters (a Rust login verifies a node-hashed scrypt entry; either runtime's logout kills the other's session). The OAuth consent URL byte-matches TS's own serializer (same client record, same state token) and a live token-exchange failure bounces `exchange_failed` with the provider's prose dead in the log; the full happy path needs a human at Google's consent screen, so claim-via-google and the org-domain/invite doors are verified to their last reachable hop, not past it. The admin invites console lands with batch 3's admin surfaces |
| 3 | **Product reads in bulk** — boards, orgs, settings, agent reads, the read side of every surface the SPA lists | byte-diff per route family; the SPA's boards/orgs/agents surfaces on Rust reads | **served from Rust** — `/api/agents`, `/api/apps`, `/api/activity`, `/api/cost`, `/api/keys` (+ `/api/keys/{id}`), `/api/teams` (+ `/{id}` and `/{id}/members`), `/api/workflows` (+ `/{id}`), `/api/notifications`, `/api/me`, `/api/models` (+ `/api/models/efforts`), `/api/admin/model-roles`, and the admin console's wave-1 groups serve from Rust: `/api/agent-role-templates`, `/api/admin/password-accounts`, `/api/admin/google-client` (+ its `/login`), `/api/admin/instance`, `/api/admin/permissions`. The teams list is the port's first ACTING-user surface: a personal assistant's `tak_` key proxies to its owner (a general agent or a rejected credential resolves to nobody — the plain 401, never agent-auth's refusal). The workflows pair is the port's first zod-ARRAY surface — the 400 table behind match/skills/toolkits (element checks outrank the array-length check, the trim runs before the length bounds, unknown keys strip at every level) is probed and pinned in `body.rs` tests, and its two quiet corners held on both sides: no 404 anywhere (a missed id updates or deletes nothing and still answers ok), and an EMPTY PUT patch runs no SQL at all — so it answers ok even for a non-uuid `{id}` while a patch with one field present takes the recorded platform-500 divergence. All byte-diffed on the same sessions for anon (401), member (403), and admin callers — the reads literally, the writes through restore-safe cycles (template upsert/shadow/delete, account create/set/remove, org-default and per-user override set/clear, login toggle, domain set/verify, key mint/policy-set/policy-clear/revoke with the caps read back through each runtime, team create/rename/delete and the add/re-role/remove member cycles, workflow create/patch/delete with jsonb key order read back through each runtime), the 405s with their `allow` headers, and the full zod 400 table including the permissions union's one non-generic message (a string userId failing the uuid format check answers `Invalid UUID`; every other both-branches-fail shape answers `Invalid input` — probed against zod 4.3.6's sole-non-aborted-branch return, pinned in `admin_permissions.rs` tests) and the nullish z.number() table behind the key policy (probed and pinned in `body.rs` tests: the safe-int guard's own bounds, a max breach that says "number" even on an int field, and 0 echoing back as 0 while the row stores unlimited). The notifications route is the port's first zod-RECORD surface — the prefs patch's whole 400 table (the record's own type word, the generic `Invalid key in record` past 40 chars that is NOT the string-max message, the enum's message for a wrong-type value, the field's two refines outranking the root's) is probed and pinned in the route's tests — and its boundaries held on both sides: the mail fan-out behind the prefs (sendGatedMail, the outbox, the drain) is scheduler plane and stays TS (rule 5, batch 5), the brief-nudge lands without its SSE publish, ids absent AND ids empty both mean "mark all of mine", the member's delivery 403 fires after the 400s and before any write so one PATCH never half-applies, and the admin delivery flips are audited identically on both runtimes (the audit rows mirror). `/api/me` migrates by EXACT pathname — its me.* siblings (`me.mcp`, `me.assistant`, `me.events`) are the fleet/agent/SSE planes and move whole with batch 5 — and carries the port's first probed ICU behavior: `isValidTimeZone` asks Intl to resolve the name, so the acceptance grammar itself was probed as a 100+-spelling table on which node's and bun's ICUs agreed on every row (IANA names match case-insensitively, `±HH`/`±HH:MM`/`±HHMM` offset forms resolve up to 23:59, `Factory` is refused) and is mirrored with chrono-tz's tzdb plus a hand-rolled offset parser, the table pinned in `api/src/me.rs` tests. The route's own corners held byte-for-byte: the zod bounds run on the RAW string and the handler's trim comes after (a spaces-only name is legal and stores `""`), the PUT applies fields in me.ts's sequence with no transaction (a name lands in the row and the live session — the KEEPTTL patch — before a refused model or zone answers the same request), the member model gate reads the allowlist and the catalog exactly as model-access.ts does (a qualified id is judged by its pinned bare model; an id no endpoint serves is refused), and null is a legal VALUE for the nullable trio, distinct from absent, via `present_nullable_string_member`. The boards family moves whole to batch 4, not slice-wise here: its enqueue/reclaim corner is rule-5 territory, and a method-split boards (reads here, writes there) breaks one-runtime-per-group. Deferred to their owning batches: `/api/alerts` + admin `/api/home` (computeAlerts probes scheduler/mail/docker/MCP state that lives in the TS process until batch 5), `/api/history` (kb + artifacts read models), `/api/inference` (docker socket + the gateway pulse), `admin/invites` (createInvite sends email — batch 5), `admin/settings` (the fleet-docker import), `memory/{id}` (readMemory/writeMemory are `docker exec` into the running agent container — the same plane as `/api/inference`), and the model-identity plane crossed as the batch's closing slice — `models`, `models/efforts`, and `admin/model-roles` all stand on the harness persona engine, so they ported as one unit. The engine came with them: the persona index's load-bearing two-pass claim (base ids first, then alias tiers — the tier pass has NO main-configured gate, an alias with a valid target claims its tier id even when main is unset or malformed, while a base id without a main claims nothing; every claimed pool carries the agent's fallbacks, because the runner can land on them), the effort ladder (provider catalog plus the admin declaration, which REPLACES and never merges, `EFFORT_ORDER` with unknowns appended, the once-only backfill when a stored catalog predates the efforts extraction), the model-info matcher (OpenRouter's public catalog — full id for slashed, unique suffix for bare, dots↔dashes, 6h stale-on-failure cache — and the `model_blurbs` override read whose error PROPAGATES to the route's 500, TS `Promise.all` parity), the resolution chains (`pin→role→utility→env→first-routable` and the muse chain, the member allowlist gating role/utility/preferred/first-routable but never pin/env, `routes()` advancing the rr cursor on purpose), and the ISO-8601 pair in `agent_auth.rs` — the house has no clock crate, so the formatter gained its strict inverse `iso_to_epoch_ms` and capability's expiry reads parse without a dependency. Verified by a 37-case byte-diff on the same sessions: the three routes × anon/member/admin, the whole zod 400 table behind the PUT (role enum first, then model bounds, then effort bounds — zod's schema order), restore-safe write cycles run through BOTH runtimes (assign, effort set against the published ladder, effort clear, model null and `""` clears, effort on an unassigned role refused, an unpublished level refused with the publish sentence, the audit rows mirroring), the 405s with their `allow` headers, the effort feed's query corners (missing/empty/whitespace model, percent-encoded ids, and a DUPLICATE `?model=` — `URLSearchParams.get` is first-wins, and both sides must show it), and the persona-configured default through a live config mutation read only after both runtimes' 60-second persona caches expired. Three cases differ in array ORDER alone — the recorded localeCompare divergence, invisible until an endpoint name with a capital letter joined the registry: TS collates `Z.ai/glm-5.3` last, byte order puts it first, and both lists carry the same rows |
| 4 | **Runs engine + scheduler** — the engine core (`runs/lease`, `store`, `define`, `run`, `decide`, `reclaim`, ~3.5k lines: Redis leases with CAS renew/release Lua and per-attempt tokens, every `runs` write a CAS on `(id, lease_owner, state)`, `attempt` incremented only on a reclaim so a healthy 400-step run can never exhaust `maxAttempts`, the step-budget lease TTL, persist-then-publish ordering, and the `awaiting` guard that parks a decision rather than failing it — `run.ts` also documents two deliberately deleted exports, a second in-file reclaim sweep and an ungated `answerRun`; a Rust port that re-derives either re-creates the bug), the six run kinds behind `runs/boot.ts` (agent-hire, plan-draft, rag-backfill/rag-reindex, research, work-session — work-session drags the harness capability layer, which is where the ticket/board writes actually happen), the boards family (held out of batch 3 for exactly this: task create → `maybeDispatchTicket` → a work-session enqueue, and status moves re-enter `updateTask`'s re-dispatch branch), the `run_decision` half of the approvals census, the runs/me/boards SSE streams (one dedicated Redis subscriber per client), and the whole registered job table — comms-decay, outreach-sweep, price-refresh, daily-digest, approval-escalation, notification-mail (the module's outbox and drain, the one already-ported route whose module still holds a hand inside the scheduler), run-reclaim, daily-brief, mcp-library-refresh, update-check — plus `maybeRewriteBlurbs`, which becomes a REAL job here: its only TS trigger was the `/api/models` handler, which the coexistence proxy shadows, so the org-voice sweep is dark in any proxied environment until this batch ports it. The flip is one slice — stop-TS → arm-Rust, never both — so everything the ten jobs touch must be in Rust first, which pulls the mail transport and the enqueue-calling routes (boards writes, research decide, plan-drafts, admin.rag, fleet create/hires) into this batch's blast radius even where batch 5 had them; the engine's dependency arrow is runs → gateway (already ported), never the reverse. THE FLIP MECHANISM HAS LANDED AND IS ARMABLE: `TALARIA_SCHEDULER=rust` hands the whole schedule over (`api/src/jobs.rs` registers and arms the table, TS's `startScheduler` stands down on the same value, the models route's blurb kick retires for the blurb job, `dispatch_deps` swaps the coexistence stub for the real driver edges, and the five enqueue sites drive inline exactly as TS did); the arm still holds itself to the census's six run kinds, and that table is WHOLE — the retrieval session's reindex pair (rag-backfill/rag-reindex) crossed with the retrieval plane's own halves (artifact-routing and the health probe landed beside it), so the pair's defs, real deps, arms, and start verbs are all Rust, and the guard's empty-list assertion is now a regression tripwire rather than a checklist (agent-hire crossed with the fleet write plane: the def, its real deps, the arm, and the fleet create/hires routes). The reindex pair's last seam closed with the admin.rag route: the retrieval console crossed whole (GET health + both run projections + the 60s upgrade status, PUT the reranker config and space↔brain bindings, POST the kicks that make the pair's start verbs live enqueue sites and the live rerank model catalog), and with the upgrade-status cache on the Rust side the reindex run's `invalidate` edge got its real body — a rebuild drops the cache its own panel reads, in the process that rebuilds. The bare `/api/rag/*` family crossed with the console (the collection registry — GET list with the member picker's blanked binding matrix vs the admin's full rows, POST create with its 400-carried create sentences; the `{id}` bindings replace and delete, where the auto-collection refusal is the one 400 and every other failure is TS's platform 500; and the POST search the MCP `search_knowledge` tool rides, agent-key or session caller alike) — the registry is what the search resolves principals against, so the family crosses as one unit. `update-check` is a deliberate HOLD past the flip: its apply half pulls, rebuilds ui/dist and restarts the TS server — choreography that cannot rebuild or restart the Rust binary, so an update driven from Rust would leave the two artifacts diverged with a green checkmark; its state/check/reconcile halves stay reachable through the unproxied admin routes, manual apply keeps working there, and the auto half returns when batch 7 settles the two-artifact restart topology. **Schema ownership hands to sqlx here** (TS `MIGRATIONS` freezes, future migrations are sqlx's) | a run resumed across a restart; the scheduler armed in Rust with TS disarmed — the lease handoff; a migration issued from Rust applied once under the same discipline: the same `schema_migrations` shape (integer id + sha256 of the whitespace-collapsed statement — the applied set is per-STATEMENT, not per-file, and sqlx's own `_sqlx_migrations` table cannot replace it), the same advisory lock `8_314_207` (a mixed TS/Rust boot must serialize on the SAME key), a dedicated single-connection migration pool that is never reaped, per-statement transactions committing the statement and its bookkeeping row together, `initSecretbox` inside the pass, and growth-only re-arm — a different checksum at an applied id still refuses to boot, and that refusal is the guard working, not a bug to soften | — |
| 5 | **The tail** — retrieval (Qdrant + TEI + hand-rolled SearXNG client mirroring `search.ts`), SSE fanout (Redis pub/sub, id-shaped payloads), chat/channels, uploads, email, fleet rendering | every remaining `/api/*` prefix served from Rust; `PREFIXES` reduced to `/api/` | **in flight** — the knowledgebase plane crossed whole: all twelve kb.* route files (spaces list/create, space get/patch/delete with its editors grant matrix, the space's doc tree + create, doc get/patch/delete with rag routing, comments list/create, comment resolve/delete, backlinks, move, live presence, search, and the two public slug reads), with the engine modules they share — kb's save/version/officialize cycle, the ACL engine (`kb_perms`, shared shape with the rag collections registry, which is why the family crossed as one unit), comments, and the OKF digest debounce. The byte-diff's 107 cases ran on the same sessions for anon (401), member, admin, and a real agent caller (a dev assistant's `tak_` unsealed read-only — `ensure_agent_api_key` reopens the existing key without rotating it): the reads literally, the writes through a 28-step restore-safe cycle run through BOTH runtimes (space create/patch/editors-grants/delete, doc create/save/move/officialize→un-officialize, comment create/resolve/delete — the OKF debounce's early-return on a just-deleted doc probed from inside the same cycle), the 405s with their `allow` headers, and the full zod 400 tables — including the spaces POST's silent unknown-key strip (a `body` member on create is ignored; a doc's body door is its own patch, never the space's) and set_editors' TEXT principal_id (agents' principals are model names, so that one bind takes NO uuid cast). Four port bugs the unit tests hadn't caught, all found by the diff and fixed: the owner_user_id uuid binds (PG does not implicitly cast a text bind into a uuid column — both inserts 500'd until `$n::uuid`), the search hit's wire shape (searchDocs pushes the RAW SQL row — select-order keys, `rank` selected last, a field no TS interface names but the wire carries), the FLOAT4 rank decode (ts_rank is float4: sqlx refuses it as an f64, and f32→f64 promotion prints `0.09910322153520584` where JS's parseFloat prints `0.09910322` — the faithful path is the one JS takes, `::text` then parse), and the routing 400's sentence (TS's catch-and-answer routes put the BARE PG sentence on the wire; sqlx's Display wraps it — `pg_message` in error.rs digs the sentence out, for this site and every one like it). One expected diff: the recorded platform-500 on a non-uuid `{id}`. Next across, the artifacts family + uploads — eleven routes over THREE proxy prefixes (`/api/artifacts` does not prefix-match `/api/artifact-folders`: the match is character-by-character, `s` vs `-` at position 13, and `/api/uploads` is its own family the file artifacts point at): the artifacts list/create, `/for`, the two public slug reads (the bare subset — kind, title, icon, body, updatedAt — and the file download that wants `visibility='public'`, a file kind, and a non-empty storage ref before it will serve), `{id}` GET/PUT/DELETE with its links pair and the Google Drive export, the artifact-folders pair, and the uploads POST/`{id}` GET. The engine crossed with them: the write half of artifacts.ts (create/save with the version snapshot and the official→KB mirror — the batch-4 recorded no-op is now the real `save_doc` through `qdrant::real_deps`/`embed::real_deps`, so an official artifact's re-mirror reaches the org brain from either runtime), folders, links, the first-publish slug mint (kb's `random_slug`, shared), uploads' write half — the streamed multipart read is capped BEFORE it buffers (a declared content-length over the 25 MB cap plus its 64 KB envelope answers 413 without reading the body; Malformed and NoFile both answer `no file`, the distinction is the log's) — `save_upload` through the SigV4 `storage PUT`/local-disk write, and the Drive export stack (google_drive's multipart upload, the org connection's token half and targets, and `resolve_agent_google`: a personal assistant acts strictly as its owner's connection and NEVER the org's, a general fleet agent as the shared org account, whose Drive parent is the provisioned Shared Drive). The corners held: every route parses in TS's exact order (the links gate runs user→artifact→canRead BEFORE the body parse; `{id}` PUT fetches the artifact first, so a missing id 404s before a bad body 400s; uploads checks the perm before it opens the form), the folder Patch's editors parser is LOCAL — no role default, no principalId max, no array cap; the kb shape is the doc's, not the folder's — and a folder create with visibility omitted stores `private`, not the engine's `org` default. Two more port bugs the diff caught and fixed: `artifact_links.target_id` is TEXT (three engine queries carried `::uuid` casts — `/for` answered `operator does not exist: text = uuid` until the binds went bare, the same family as set_editors' principal_id), and the PUT's title tri-state (the Patch's title is string-optional, never nullish — an absent title mapped to set-null and a visibility-only PUT 500'd on the not-null column; absent means don't-touch, and `Some(None)` is unreachable from the wire). The agent corners: an agent-key PUT strips sharing whole (visibility/editPolicy/editors/official/ragRouting — an editor agent edits content, nothing else), and the export path rides `refuse_legacy` before any of it. Byte-diffed on the same sessions for anon/member/admin: the reads literally (the whole list, `{id}`, `/for` with a live link row), a full write lifecycle through BOTH runtimes (folder create → artifact create into a named root folder → PUT rename+icon → PUT publish with the slug mint → the public slug read → folder PUT/GET → the deletes) normalized identical, the uploads multipart roundtrip (the bare Attachment JSON — not wrapped — plus serve headers and bytes), the 405 `allow` tables, and the port's first proof against production Google: a live Drive export from both runtimes creating real Docs with the identical `{file}` shape. Next across, `/api/history` — one route over two stores, the version-history read plane: `internal_versions` snapshots (skill, memory, kb-doc, kb-space, artifact, template) and `agent_versions` (soul, config, personality). History serves FULL content, so the route's spine is the live item's own read model applied backwards — `canReadSnapshotHistory` re-enters the ACL engine for every kind (artifact and kb-space through `can_read` + `list_editors`, kb-doc through `effective_doc_perms`, memory and skill through admin-or-`ownsAgent`, template admin-only) inside the TS try/catch, so every miss and every error reads as 403 — fail-closed is the whole point, or history is a bypass of the permission system. The engine bits crossed with it: internal-history's read half (the list's `size` is PG `length(content)` — characters — and `createdAt` rides the epoch-ms→ISO path), the two personal-agent helpers the gate borrows (`personality_of`'s marked section with its `?? ''` fallback, `owns_agent`'s catch→false), and `list_versions` gaining the `createdAt` it already selected in TS. `kind=config` serves `stringifyYaml(config)`, and those TS bytes are the wire contract — so the slice dragged the port's first serializer with it: `api/src/yaml_string.rs` is a source port of the npm `yaml` package's stringify (fold arithmetic in UTF-16 units, the block/folded/quoted dispatch, core-scalar quoting, `Number::toString` formatting), pinned byte-for-byte by 145 fixtures the REAL package generates (`scripts/gen-yaml-vectors.mjs`, the secretbox-vector house pattern, `api:vectors:check` as the staleness tripwire). The byte-diff's 43 cases ran on the same sessions for anon (401), member (403s — and the org-visible reads a member CAN make), and admin: every snapshot kind's list and single revision, the three version kinds × list and `?rev=` (on an owned agent and an org agent — the live config revisions exercise the emitter end to end, nested `raw.model` maps and all), the parse corners (empty owner/name/id are ABSENT — JS falsiness; an empty `rev` lists, it doesn't select; a non-uuid rev on a version kind 404s while a snapshot kind takes the recorded platform-500), and the 405 with its `allow` header. One port bug the diff caught and the unit tests hadn't: `length(content)` is INT4 — sqlx refused it as an i64 and every list 500'd until the bind matched the column. Next across, the channels family — the comms plane's ten route files crossing whole under one prefix (`/api/channels`, which subsumes the plan-draft mount's old SHAPES entry): list/create, `{id}` GET/PUT/DELETE, the agents pair, conclude, the events stream (the port's next dedicated-subscriber SSE), the members pair, messages GET/POST, the message PATCH/DELETE, reactions, and read. The engine crossed with them — channels.ts's insert/list/edit/delete, members, agents, the reaction toggle, read cursors, conclude (the relay-summary run and its 502 nothing-to-conclude), archive/hard-delete with the detached qdrant activity purge — plus the reply plane channel_replies.ts drags along (the @mention notify, the DM peer notify, trigger_agent_replies with its streamed agent replies) and the titler the summary run reads. Two TS wire shapes hide in the one family: the insert's RETURNING selects NO guard/editedAt (both keys ABSENT on a POST's echo) while every list path selects both (PRESENT, null when unset) — one serde struct serializes the page shape and `inserted_wire` projects the insert shape by removing the two keys, because skip_serializing_if would have broken the page. The agent GET strips `guard` outright: agents read channels through this route (the MCP `read_channel` tool proxies it), and a finding is flagged content's verbatim excerpt — the one thing that must never re-enter a model's context. The byte-diff's 102 cases ran on the same sessions for anon (401), a member (403 on channels she's not in, the reads through the one she is), and admin: the reads literally across the channel kinds (general, group relay, DM), the writes through a restore-safe cycle run through BOTH runtimes (create channel and relay → post → thread reply → patch own → react on/off → read cursor → list/detail/thread-page → conclude on a channel (its 400) and on an empty relay (its deterministic 502) → hard delete), the 405 `allow` tables, and the zod 400 table with its parse-order probes (a bad POST body 400s BEFORE auth; the PUT's owner gate runs before its body parse). Three port bugs the diff caught and the unit tests hadn't: the edit's bind index (`$3` named with two binds — every PATCH 500'd), the PUT name's min bound (an empty name is the min failure, not a value), and the fractional `since` (TS 500s — the fraction reaches the int4 comparison uncaught; the port first floored it). Next across, the conversations family — the durable chat itself: `chat.rs` POST (the family's only EXACT-path proxy entry, whole-path `/api/chat`), the conversations list, `{id}` GET/PATCH, and `/api/dms`, the last two as prefixes. The engine crossed as its own module: `chat_persist.rs`, the persist half of chat-persist.ts — the gateway stream is TEED through a bounded mpsc whose receiver becomes the client body (`Body::from_stream` over an unfold, no extra dep), so the drain that persists survives a client hang-up the way the TS pipe-through does; the 400ms throttled flush (the first event immediate), the ledger's real-usage-or-estimate, the guard block AWAITED before indexActivity and notifyPlanMentions because both take a copy of `content` (strict mode's redaction must land first — the brain and the mail are the two places an unredacted reply must never reach), the Titler's first-exchange rename, and the queued-message continuation chain (the in-process CONTINUING set, the queued message's OWN effort re-read off its row and re-validated against the model it will actually run on, a stale tier degrading to the base model rather than dying — a chained turn nobody is watching). The continuation chain needed the port's first real borrow-checker seam: continue → persist → continue is an opaque-type cycle (E0391) that Box::pin and Box<dyn Future> both fail to break — only routing the spawn through a plain non-generic fn (`spawn_persist`) does, and its call site documents why it is load-bearing. The chat route's own order is the TS order verbatim: accessibleConversation (the WHERE clause is the whole rule — research conversations 404 at this POST gate; their access follows the run in get_conversation, which is why the DETAIL route serves them), the owner-aware agent gate, tier routing (`unknown tier "{t}" for {model}`), effort validation with the persona-configured default, attachment/ref resolution, titleFrom, plans.create for a new plan, the queued 202 with its immediate continuation, and the message assembly — plan system prompt + routing block, research prompt, the HANDLE_TURN_NOTE when the turn mentions a `«secret:…»` relay, multiVoice author prefixes, image parts only when imageUrls is non-empty. RESPONSE_HEADERS gained x-conversation-id and x-message-id: the SPA learns a NEW conversation's id from that header, and a first turn that didn't carry it would strand. The byte-diff's 69 cases ran on the same sessions, with one new determinism trick: the fleet manifest's live agents (gregosaurus, margaret — containers up) answer nondeterministically, so the streaming cases ran on inbox-assistant, a real agent_model NOT in the manifest — both runtimes answer with the instant canned mock stream, byte-identical including the SSE framing. The reads literally (list chat/plan/garbage-kind, detail across chat / plan-with-collaborator / plan-without / research-via-run, non-member and anon 404s), the dms table (existing DM byte-identical, self-DM 400, create normalized, the FK-violation 400 whose message is the BARE PG sentence), the zod 400 table with its parse-order probes, the agent gate through a synthetic `user_agent_access` window granted and revoked inside the harness, the write cycle through BOTH runtimes (first turn → second turn into the same conversation → queued 202 → rename → a SQL-granted collaborator's rename and read), the SSE headers with the per-side uuids normalized, and the 405 allow tables. Three port bugs the diff caught and the unit tests hadn't: messages.guard is the NULLABLE column of the detail select (every `{id}` read 500'd until `Option<Value>` — the TS wire carries null, not `[]`); ensure_dm's FK path put sqlx's DECORATED message on the wire where postgres.js puts the bare sentence (`pg_message`, wired through the DM's six error sites); and the SSE response's cache-control belongs ABOVE content-type on the wire — header order is part of the byte contract. A live-agent turn (gregosaurus, containers up) proved the real path end to end: the stream, the persisted complete reply, and a usage_events row with the REAL counts (est=false, 21820/110). Next across, the inbox.focus family — the assistant's whole attention plane under one prefix: the queue (GET `/api/inbox/focus` — four sources concatenated in FOCUS_SOURCE_TYPES order: pending approvals, failed/needs-decision tasks, unread channel/DM comms, actionable notifications — with bucket counts and the assistant's brief cache), the badge summary, the state write (PUT `/state` — viewed, snoozedUntil set/clear, the snooze echoing its fresh timelineEntry), the actions (POST `/actions` — mark_read, snooze, the task actions with their confirmation tokens, stale→409 and failed→422 exactly as actions.ts maps them, and the confirmation reissue), the command stream (POST `/command` — the SSE plane that runs one instruction through the focus assistant in normal/fast/plan mode, conversation/status/content/activity/done/error frames), and the segmented conversation picker (list, create, `current`, `{id}` GET/DELETE-archive). The engine crossed as four modules — the queue builder (buckets, fingerprints, decisions, briefs), the four source reads (ACTIONABLE_NOTIFICATION_KINDS, `accessible_notification_hrefs` lifted pub(crate) out of the daily brief — both surfaces ask the same question), the action policy table, and the timeline — plus the approvals source's read half (google_pending_actions) and the conversation runner, whose harness def is the STREAMING seam's first caller: `dispatch_transport` replaces `gateway_transport` (pickTransport's gateway-or-fleet dispatch by transport_kind) and `StreamOptions` is the streaming surface the focus assistant rides. The family's one piece of process state is the FOCUS LOCK: TS holds a per-user mutex across the command generator with a `finally` release; the port's guard MOVES into the stream task so it releases with the stream — the same lifetime as ownership — and the 409 sentence is verbatim. Recorded divergence (documented at the route): on a client disconnect TS stops consuming and leaves the assistant row `streaming`; the port's run keeps going and persists the completed reply — the chat family's tee philosophy. One proxy prefix carries the family whole (eight paths, the queue read through the `{id}` archive) because the lock spans the command stream and the state route — a split family would split the lock. The byte-diff's 31 cases ran on the same sessions for anon (401), member (her own queue and summary), and admin: the reads literally (queue, summary, the picker list), the validation shapes (empty and bad-type state PUTs, a gone source, a gone action key, a short confirmation token, a modeless and a bad-mode command), the writes through per-side seeded rows (the runtimes share one DB and mark_read CONSUMES its row, so TS reads notification A and Rust B — same shape, different ids), the conversation lifecycle (create per side, read by id, current, a bad cursor, archive on its own id), and three SSE command streams — the conversations family's determinism trick extended: a catalog model with NO fleet-manifest row (outcrop-labs/qwen-3.8-27b) answers through the canned-mock path on both runtimes, so the streams diff byte-for-byte including the SSE framing. Six port bugs the diff caught and the unit tests hadn't: the command frames' FIELD casing (an enum's `rename_all` camelCases only the variant TAGS — `conversationId` rode the wire snake_case and the panel's reader silently lost the id on every frame; pinned by a unit test now because the compiler cannot see it), the picker's two param-type 500s (`max_seq` is INT4 against an i64 field — `::int8`; `left(m.content, $2)` with an i64 bind — `::int`, PG will not take bigint where `left()` wants int4), the archive's `returning id` decoding as UUID against a String field (`::text`), the approvals source's MEMBER LEAK (TS splices the admin arm as SQL TEXT — for a member the clause VANISHES; the port's first shape bound is_admin, and `or is_org = $2` with false matches every OTHER person's personal actions — members would have seen strangers' pending approvals in their queue; the literal `or ($2 and is_org = true)` short-circuits to no rows instead), the picker summary's key order (preview LAST — a TS spread appends the new key), and the command stream's header order (cache-control → connection → content-type: TS builds the response through a fetch Headers object, which iterates ALPHABETICALLY, so the wire does not match the route literal's spelling — chat.rs already records this). Next across, the brief family — the assistant's morning document and everything a person does to it: five routes under one prefix (`/api/brief` carries the document read, the line verdict, the read cursor, the delegation pair, and the draft reply whole, because the read's sweep-if-due must land on the same process as the engine that owns the brief's state machine). The module already held the writer — batch 4's daily-brief job (`open_brief`, `sweep_brief`, `run_brief_pass`) and the draft plane the notifications family dragged — so this slice is the reader those routes stand on: `get_brief` (sweep-if-due then read — the GET runs the per-user sweep first, a sweep failure is logged and swallowed, and the document still serves), the absent literal in TS key order `{absent, nextAt, agent}` (every absence names the agent — the surface renders the same header from the empty state — and `pending` is the only kind whose nextAt is knowable; `writing` and `no-agent` answer null) with the `?tz=` param discarded when unparseable and the stored zone winning, `mark_brief_read` (the cursor only ever advances — `greatest(read_seq, $seq)`), `mark_brief_item` with its three 404 sentences ("that line is not on today's brief" with its U+2019 apostrophe — bytes the surface renders verbatim, not prose to re-type; "nothing to restore this line to" when every row the line ever had is terminal; the fallback "could not update that line" — 404 rather than 400 because the request was well formed, the line just is not on today's page, usually a stale tab from yesterday), and the fold as typed structs in TS declaration order (`view`/documentView — lines keyed by source_key, terminal at resolved|checked|dismissed, sections in BRIEF_SECTIONS order each sorted unresolved-first then p0/p1/p2/else-3 then the seq the log first put the line at, unseen = seq > read_seq && kind != lede — a resolved line sinks but is never dropped, because the thing a person read this morning has to still be findable this afternoon, struck through). The delegation trio crossed with it: the grants list/grant/revoke — owner-only by construction, not an admin Perm, no route that takes a user id, and `grant_reply` re-checks channel membership rather than trusting the handler, because a grant on a conversation the owner is not in would let an agent speak in a room its owner cannot read (a foreign channel answers 403 "That is not one of your conversations."); the grant's side effect (granting permission to send a reply that is already written MEANS sending it — `release_drafts` fires the parked drafts, a send failure swallowed to 0 exactly as the TS `.catch(() => 0)` because the grant itself stands and the sweep will still draft); and `decide_draft`, the one route in the family that causes something to LEAVE — a message another person receives, so it is the one that refuses on staleness: a gone draft 404s "That draft is no longer available.", and a draft whose conversation has moved answers 409 with the exact sentence "They have said something since this was written, so it would answer the wrong message. Ask for a fresh draft." — 409 rather than 400 because the request was well-formed and was correct when the person read it; the world moved underneath it, and the client's answer is a fresh draft, not a fixed input. The byte-diff's 39 cases ran on the same sessions for anon (401), member, and admin, with one new determinism problem and its answer: the brief is ONE DOCUMENT PER PERSON PER DAY, so mutations cannot be seeded per side — the harness warms the shared document first (the open is a real model turn that writes the lede, and the sweep stamps last_swept_at), then works per-side probe rows (six seeded lines folded through both runtimes' reads, per-side channels and drafts for the grant→release(sent:1)→revoke cycle, the foreign-channel 403, and the reply ladder gone/stale-409/approve-sent/reject) plus SEEDED RESOLVED PAIRS for the fold's rendering, each runtime's check-off proven by a post-write read on its own side, never across. Four port bugs the diff caught and the unit tests hadn't — two known families: postgres uuid columns decoded as Option<String> need `::text` (the entry row's `batch` and `supersedes`), and INT4 seq columns decoded as i64 need `::int8` (the seq claim's returning `last_seq`, mark_brief_stale's returning, the artifact mirror's `read_seq`) — the same uuid-as-text and int4-as-int8 casts the kb and history families each paid to learn, caught here on the harness's first run. One absence recorded rather than byte-tested: the `pending` kind is unreachable at the hours the harness runs (no stored zone is before its open hour), so it is pinned at the engine level only. Next across, the secrets family — the working vault: six routes under one prefix (`/api/secrets` carries the collection GET/POST/PATCH/DELETE, the folders pair, reveal, share, relay, and git-credential whole, because the resolve path the reveal serves is the same engine MCP tool-call substitution spends through — a split family would split the vault). The engine crossed as its own module (`workspace_secrets.rs`): `SecretDoc` in exact TS field order (keys and labels only — a LISTING never carries a value, only an explicit reveal does), the resolve/reveal refusal ladder (unknown→404, not-shared/not-revealable→403, else 409), `credential_for_host`'s entry selection (`username`??`user` for the login, `password`??`token`??first-entry-that-isn't-the-user for the pass — a single-entry doc IS a token and answers `x-access-token`, which is why a one-entry relay serves git at all), the spend's double audit, `mint_relay` (random name, uses=1, the one-shot burn), folders (a PERSON shared a folder can reveal everything in it; an AGENT granted one can SPEND everything and read none — the asymmetry is the point), move (owner-only even for admins: the TS route passes no isAdmin, and the revealable flag decides person-vs-admin filing), and the listing (revealable ∧ owner-or-reader-or-folder-reader-or-folder-owner, created_at desc). github.rs's token half crossed with it: `app_jwt_at` (iat backdated 60s, exp +9m, JSON.stringify literals, RS256) pinned by the port's third cross-language vector file (a committed RSA-2048 PEM, three clock instants across the 2038 boundary — `scripts/gen-github-jwt-vectors.mjs` in the secretbox house pattern), and `agent_git_credential` as the git helper's second drawer (`github:{repo}` as x-access-token); github's REST half waits for workbench. RESPONSE_HEADERS gained pragma and referrer-policy — the reveal answers with the full no-store/no-cache/no-referrer triad. THE INTEROP PROOF IS THE CENTERPIECE: both runtimes share one DB, so the harness seals the same literal through EACH runtime and reveals it through BOTH — four crosses, the same value back every time — which is the whole reason the secretbox port exists: the ciphertext is written by whichever runtime sealed it and read by whichever opens it, forever. The byte-diff's 127 cases ran on the same sessions for anon (401), member (the refusal ladder from outside), and admin: the full zod 400 tables (the entries array's elements-before-length order, the entry key's regex-before-max, both union near-miss tables), the refusal ladder, person share→reveal→unshare→refused-again, agent grant/revoke, the expired refusal, the relay lifecycle (mint → not-revealable → not-deletable → git spends it once → the second ask 404s), the folders lifecycle, the listings, and the owner deletes — run per-side through solo captures, because a shared DB gives a delete only one winner. Three port bugs the diff caught and the unit tests hadn't: `select distinct s.name … order by s.created_at` (PG requires the ORDER BY expression in the select list — every listing 500'd until the query selected both columns, as TS always did); the PATCH's admin thread (the port passed `role == "admin"` where the TS route passes nothing — moveSecretToFolder is owner-only by design); and the union null-body sentence (zod v4 unions flatten EVERY failure to the same two words, a body that isn't an object included — the house `as_object` sentence leaked through the folders and share routes). One determinism note for whoever runs it next: the agent key is re-fetched through `ensureAgentApiKey` (it reopens the existing key rather than rotating), because a parallel session's re-mint had already staled the saved plaintext. Next across, the integrations/google family — twenty-one routes under one prefix (`/api/integrations` carries the whole OAuth pair, the org plane, the user surfaces, the approval queue, and the agent surfaces, because every door reads the same connection rows — a split family would split the connect state): the personal connect+callback pair, the org plane (its own connect pair, the status+targets GET/PUT with the disconnect, provisioning read+run, and the live health probes), the per-surface user reads (calendar, drive, gmail — each with its query-param fold: drive's `?q` is `?? undefined`, so an EMPTY q rides in, while gmail's folds `|| 'in:inbox'`), the three user writes that all draft through the approval queue rather than send (gmail send, calendar create, drive import), the pending queue (list + decide), and the agent plane (calendar, drive, gmail list/send, labels, organize, one message by id). The engines crossed as four new modules — gmail's listing/labels/organize/send (the listing's metadata query is built with a form serializer), the API health prober, the shared `google_fail` mapping (it lives in its own module, NOT error.rs — the port learned this the hard way), and workspace provisioning — plus the halves earlier slices had left behind: the artifacts family crossed the Drive export stack's read half, the inbox.focus family the pending queue's read half, and this slice finishes both (the OAuth state mint and the callback's five-rung refusal ladder — no cookie, `error=`, a BLANK error, a mismatched state, a member touching the org door — each a deterministic 302; the org targets patch's tri-state where absent leaves the column alone and null clears it; the pending decide with its member-403 and terminal-409; and the agent send that queues a draft whose verdict sentence names the approver — admin for the org, owner for a personal assistant). The agent plane's own gates ride in TS's order: `refuse_legacy` before anything Google-shaped, the `x-agent-name` must-equal-`model` check, then `resolve_agent_google` — a personal assistant acts strictly as its owner's connection and never the org's, a general fleet agent as the shared org account, and neither is 409 not_connected with the exact sentence. The port's first silent-trait lesson lives here and is worth the telling: `url::form_urlencoded::Serializer` is not `Send`, and one held across an await inside an ENGINE fn makes every handler that awaits the engine fail E0277 with no body error — the serializer is now scoped and `finish()`ed before the await, and the trick that named the line is `#[axum::debug_handler]` (axum's `macros` feature, added temporarily; it points at the exact await the way the compiler would not). The byte-diff's 69 cases ran on the same sessions for anon/member/admin plus two agent keys: the personal surface is deterministic (no personal row exists — not_connected is a certainty, the DELETE aside), the org plane and provisioning read (org agents only — the `.filter((d) => !d.ownerUserId)` cut, personal assistants send as their owner), the connect doors with `x-forwarded-host` pinning ONE OAuth origin for both runtimes so the redirect URIs derive identically across the port split, the callback ladder, the full zod 400 tables, gregosaurus's 409s alongside the no-auth 401 and wrong-name 403, margaret's LIVE reads against the real org connection (calendar, drive ±q, gmail ±q, labels, one full message by a shared id — the port's second proof against production Google), organize's validation table plus the destructive-label refusal, org health's live probes, and the one designed round-trip: an agent drafts a gmail send, a member's verdict is refused, the admin's reject retires each side's own row, deciding again answers the terminal-state 409 — nothing ever leaves the building (the destructive verbs — personal/org disconnect, the targets PUT, a real provision run, a VALID send/create/organize, a live drive import, an approve, which EXECUTES — are SKIP-listed, batch 4's posture). Two port bugs the diff caught and the unit tests hadn't: the targets wire's key order (a manual `json!` map, and `sharedDriveId` alphabetized ahead of the TS order — the fix embeds the typed `TargetsWire`, whose declaration order IS the wire order, the preserve_order lesson in its cleanest form), and the provisioning agents filter (`owner_user_id` is a UUID column and needs `::text` as a selected fifth column — the first attempt destructured `email_alias` as the owner flag, which seated gregosaurus in the org's send-as table). Still ahead in this batch: the mcp family with the last SSE route, workbench, the admin tail, the fleet tail, and the remaining singles |
| ∎ | **Cutover** — the TS API deleted (`ui/src/routes/api` and the backend of `ui/src/server`); the SvelteKit server keeps serving the SPA and proxies every `/api/*` request to Rust (SSE and scheduler included); the prod image gains a Rust build stage (musl static — devboxes are glibc, prod is alpine) | `ui/src/routes/api` gone and `ui/src/server` reduced to what the frontend server itself needs (the handler and the proxy hop); `PREFIXES = ['/api/']`; `bun run start` still boots the SvelteKit server, now with the Rust api as its backend process; the SPA fully usable end to end with no TS API behind it | — |

## Recorded divergences

Everything found while porting where Rust cannot (or should not) match TS
byte-for-byte. This list is the contract: a divergence not here is a bug.

- **Model-list ordering.** TS sorts `localeCompare`, Rust sorts bytes. They agree
  on the ASCII lowercase ids the registry generates, and diverge the moment an
  endpoint name carries a capital: `Z.ai/glm-5.3` collates LAST under
  `localeCompare` (z after o) and FIRST under byte order (0x5A before 0x6F) —
  observed, not hypothetical, in the `/api/models` and `/api/admin/model-roles`
  diffs, where the two lists carry the same rows in different order. Models
  order isn't contractual (a picker's display order). (`gateway/models.rs`,
  `model_access.rs`)
- **App discovery reads disk.** TS discovers apps via build-time
  `import.meta.glob` and sorts `localeCompare`; the Rust port reads
  `apps/<slug>/talaria.json` from disk and sorts bytes (`api/src/users.rs`,
  `appViewRoutes`). Same directory in dev; the difference needs a build that
  compiled an app in and then lost its source tree — not a reachable state.
  The same entry covers the `mcp` flag (`api/src/users.rs`, `enabledApps`):
  TS asks its build-time glob whether `apps/<slug>/mcp.ts` was compiled in;
  Rust checks the file exists on disk.
- **Uncaught TS route errors answer the platform's plain-text 500; the port
  answers the house envelope.** Same status, different body: TS's server has
  no catch-all boundary, so a route throw the handler didn't catch (a non-uuid
  `{id}` reaching a raw SQL bind on `/api/keys/{id}`, a client-id that trims
  to nothing on `PUT /api/admin/google-client`, a fractional `?since=2.5`
  reaching the int4 comparison on `/api/channels/{id}/messages`, a non-uuid
  `{id}` on `/api/conversations` GET or PATCH) surfaces as
  bare `Internal Server Error` text. The port logs the cause and answers
  `{"error":"internal error"}` — rule 8's discipline applied to our own
  failures (`api/src/routes/keys_id.rs`, `admin_google_client.rs`,
  `channels_id_messages.rs`, `conversations_id.rs`).
- **Corrupt scrypt rows fail closed here, 500 there.** TS parses hash-entry
  numbers with `Number()` (admitting `"1e4"`, hex) and Node's base64 decoder
  decodes past junk, so a mangled `scrypt$…` row can reach node:crypto and
  throw; the Rust parse rejects the same row and the login is a plain 401
  (`api/src/password.rs`). An entry that malformed was never a credential
  anyone could present.
- **`/api/auth/providers` answers `configured: true` unconditionally.** TS
  computes it from env presence (`DATABASE_URL && REDIS_URL`); the Rust process
  refuses to boot without both, so the "not configured" warning the flag exists
  to surface has no state to describe here (`api/src/routes/auth_providers.rs`).
- **Clearing the instance domain works here and cannot on TS.** postgres.js
  turns `sql.json(null)` into SQL NULL, so TS's `PUT /api/admin/instance
  {"domain":null}` violates `app_settings.value`'s NOT NULL constraint and the
  route's validation catch leaks the raw Postgres sentence as a 400 — the
  domain can never be cleared at all. sqlx encodes a real jsonb null, so the
  upsert lands: 200, `{instance: null}`, and both runtimes' readers see null
  (`api/src/instance.rs`). The TS catch was built for the validation throw;
  the DB-failure leak is the bug this port does not reproduce.
- **The org-voice blurb sweep does not run from Rust — and coexistence
  already silenced it in TS.** `maybeRewriteBlurbs` is not a scheduler job:
  its only trigger is the TS `/api/models` handler, a 10-minute-throttled
  detached kick. The moment `TALARIA_RUST_API_URL` is set, the proxy answers
  `/api/models` from Rust and that handler never runs — so the sweep is dark
  in every proxied environment (including dev-with-env), not just after
  cutover. The Rust route reads `model_blurbs` rows but never writes them;
  the sweep ports as a real registered job with the scheduler (batch 4).
  Until then the degradation is cosmetic and self-healing (the rows are read
  the moment anyone writes them), and unproxied TS installs keep firing it.
- **The blurb clamp cuts at a char boundary, not a surrogate boundary.** TS
  `slice(0, 157)` counts UTF-16 units and can split a surrogate pair in half
  (a lone surrogate in the response); a Rust string cannot hold half a pair,
  so the clamp stops at the last whole character that fits. Reachable only
  with a >160-unit first sentence whose 157th unit falls inside an astral
  character. Cosmetic. (`api/src/model_info.rs`)
- Nothing else yet.

## Layout of the crate

`api/src/` — `main.rs` (axum serve, graceful shutdown), `config.rs` (env, the
secret-root precedence `TALARIA_SECRET_KEY` → `_FILE` → `AUTH_SECRET`),
`error.rs` (the two envelopes, byte-tested), `db.rs` (pool; the schema-ownership
comment), `state.rs`, `auth.rs`, `ratelimit.rs`, `secretbox.rs`;
`routes/` (one file per prefix, mirroring `ui/src/routes/api` shapes);
`gateway/` (the LLM gateway's internals: registry, vault, provider, params,
usage, budget, guard). `Cargo.lock` is committed, as `bun.lock` is.
