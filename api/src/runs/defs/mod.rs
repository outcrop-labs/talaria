// The run kinds — one module per TS file under ui/src/server/runs/defs/. A
// kind registers from the module that owns the work, next to the thing it
// drives; the registry itself is define.rs.

pub mod work_session;
