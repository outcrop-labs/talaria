// Workchain routes — the chain-scoped half of the boards family. The
// board-scoped list/create lives under routes/boards/boards_id_workchains.rs;
// this module is everything addressed by workchain id.

pub mod workchains_id;
