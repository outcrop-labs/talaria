- **Workchains: one chain fills the view at a time.** The fourth lens stops stacking every chain into one scroll: a chain switcher above the canvas names the focused chain with a `2/5` position read (the board filter still decides which chains it offers), and ONE node canvas takes the remaining view height — the wiring editor reads as a surface, not a scroll. The switcher dropdown also carries the management verbs for editors: Rename… (a prompt seeded with the current name), Delete… (the typed-name double opt-in; its tickets stay on the board, unchained), and the New workchain button sits beside it. The canvas header keeps pause and zoom, and grew a rename popover (a pencil on the chain name) in managed contexts. Wheel zoom now needs a modifier (ctrl/⌘ — a trackpad pinch counts): a bare wheel scrolls the page again, one-finger touch scrolls too, and the canvas sizes to its parent instead of a fixed 420px. UNCHAINED stays below the canvas, unchanged.

  Verified: `bun run typecheck` 0 errors (2 pre-existing a11y warnings in WorkchainCanvas, untouched), 1278 ui tests pass (79 files) including the new pickFocusedChain suite; `bun run check` green.