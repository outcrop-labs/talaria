- **The watch splits the agent from the harness.** The run-detail Live
  tab is Agent: Hermes replies and its own tool calls, including the
  retained ones after the session ends. Turns is the harness exchange —
  the ask, the harness output, and the agent's reply to that output.
  Verified: `bun run gate`, including `work-watch.test.ts`. Not clicked in a
  browser: no local stack, and the dogfood database has no work session to open.
