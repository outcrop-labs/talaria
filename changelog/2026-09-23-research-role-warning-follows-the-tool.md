- **A research-role warning no longer blames a model for lacking a browser the harness already supplies.** Assigning a model with no native web search (deepseek-flash, and the same shape) told the admin research runs would answer from memory and invent citations. Web search here is a tool — Talaria's `web_search`, Hermes's, or a registered server — so the warning now asks whether the run can reach search. A tool that fetches drops the warning; the capability chip still says the model cannot browse on its own, and the slot says which tool fetches. With no tool, the sentence names that gap ("no web-search tool is available here") instead of the weights. Verified: `bun run verify`, and on the 1.97 pin `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, and `cargo test` (37 suites, 89 passed, 76 ignored). The three sentences are pinned in `talaria-capability-reach` and the role-assignment tests. The live API on :5274 belongs to another worktree, so this was not clicked through in a browser.
