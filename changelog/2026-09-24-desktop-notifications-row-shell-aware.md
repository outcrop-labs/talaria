- **Desktop notifications row is now shell-aware.** The notifications settings row renders its browser variant only where that variant can be followed: the Tauri desktop shell's webview reports `Notification.permission` as `denied`, and the browser recovery advice ("allow them in the browser's site settings") is impossible to act on there — there is no such surface inside the shell, and native notifications are deferred (docs/DESKTOP.md). Inside the shell the row now keeps its place and title but states the deferral plainly in one line (no toggle, no push status, no browser recovery advice); in a browser the row is unchanged — the same permission states, the same recovery advice, the same push subscription toggle. The gate is `canUseBrowserNotifications` in `ui/src/lib/browser-notify.ts`, pure over `{ permission, inDesktopShell }` so the node vitest suite never loads `desktop-shell.ts`'s dynamic Tauri import.

  Verified: `bun run check` green, `bun run test` in ui/ green — 78 files, 1275 tests (the gate's own 2-test block covers the shell reading 'denied' — the exact case — plus every other state, and the browser states). `bun run typecheck` 0 errors, 2 warnings (both pre-existing in WorkchainCanvas.svelte, untouched). Browser behavior is unchanged. There is no component-level test coverage by design: the house vitest config excludes `src/routes/**` (file-based routing makes a `.test.ts` under routes/ a route, not a test), so the branch's coverage is the pure gate's unit tests plus svelte-check over the component.