- **Workchains become grabbable: the wiring editor.** The node canvas's ports and wires are now live interactions — drag out of a card's out-port pulls a preview wire that follows the pointer, legal targets highlight their in-ports (a larger hit radius on touch pointers), and a drop draws the edge (mis-drops cancel silently; a drop on empty canvas opens the inline new-ticket composer and wires the created ticket in — a mixed chain authored without a picker). Wires hover, select on click, and Delete (or the context menu) cuts the selected one. Cards drag to reposition with persistence, the canvas pans (space-drag or empty-canvas drag) and zooms at the pointer (wheel, buttons), and zoom-to-fit resets the view. Every chain now renders on the canvas — a straight chain needs grabbable ports too if it is to be extended by dragging; the rail leaves the lens (its reorder verb lives on in the task drawer). Pure halves (drop resolution, candidate sets, geometry, viewport math) live in `ui/src/lib/wiring-canvas.ts` and `ui/src/lib/wiring-overlay.ts` with 29 unit tests.

  Verified: `bun run check` green, `svelte-check` 0 errors (142 pre-existing warnings), 1266 ui tests pass (78 files). Live in a real browser (dev stack, seeded board): out-port drag shows the preview wire + ghost cursor and highlights legal in-ports, drop on an in-port POSTs the edge, drop on a card connects, self-drop and duplicate-target drops cancel with no write, empty-canvas drop opens the composer and Enter creates the ticket + step + edge end to end (verified via the workchains read: new step + B→new edge), wire click selects (3.5px accent stroke) and Delete removes the edge, wheel zooms at the pointer keeping the focus fixed, zoom-to-fit centers the graph, and a card drag persists its position through the PATCH.