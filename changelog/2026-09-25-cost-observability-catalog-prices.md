- **Cost observability: direct-provider usage prices from the OpenRouter catalog again, and a stale provider report stops owning the month tile.** After the observability rework, every direct-provider cloud row (openai, anthropic, …) priced at null: those providers' /models APIs are 401-unauthenticated and carry no pricing, so provider_prices stayed empty for them. The price refresh now also upserts each catalog hit into provider_prices as a model-level published rate (variant '', source `openrouter.catalog`, unique key (endpoint_id, model, variant)) — the same statement in both the rust refresh (talaria-price-oracle, reusing spend_fetch::upsert_price) and its TS twin (ui/src/server/price-oracle.ts), with failures counted and warned rather than swallowed. The month tile's provider-report override only applies while the freshest provider_spend activity row is under 12h old (2x the 6h refresh cadence); a stale report keeps the ledger month cost and `totals.providerReported` carries a `stale` flag the UI labels `(stale)`. The usage nudge also skips when a provider_prices row already prices the model. A live-rig regression test (api/tests/it/price_rig_live.rs, #[ignore]d so the unit job skips it; the api-integration workflow runs it on the real rig) pins the two facts on real tables: a direct-provider cloud row prices from the catalog row (basis `derived`), and a 13h-old activity report leaves the ledger month cost untouched. Its two tests serialize on a module-level mutex: they assert on the month-cost delta of the shared rig, and the workflow runs a module's tests concurrently.

  Verified: cargo test talaria-gateway 66/66 and talaria-price-oracle 18/18 (one new test each for the staleness window and the catalog→price row shape); ui `usage.test.ts` 8/8; live rig `price_rig_live` 2/2 (postgres only — cost_overview takes a bare pool); `bun run check` green.
