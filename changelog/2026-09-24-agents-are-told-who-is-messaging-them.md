- **Agents are told who is messaging them, every turn.** A human's message previously reached the agent as an unattributed user turn, so an agent with persistent memory filled the speaker from its remembered profile and answered the wrong person — the remembered user, a name from a different conversation (TALA-77's misattributed reporter; two desktop reports of the same shape in one day). Every human→agent door now stamps a per-turn system message naming the live sender, sourced server-side from the authenticated row (name → email → "an unidentified user", never from anything the agent remembers or the client claims): the direct-chat door (live turns, queued turns, and auto-resumed turns alike), and every channel reply including ticket task rooms — the @mention path and the assigned-agent path. Automated turns (briefs, crons, research runs) carry nothing new, and history rows keep their speaker prefixes — the identity rides only the live turn's system row. The sentence lives once, in the new `talaria-sender-identity` crate, so every door stamps the same one.

  Verified: `bun run check` and `bun run gate` green (fmt, clippy `-D warnings`, targeted cargo tests). Unit tests cover the builder's fallbacks (name → email → unidentified) and the block's shape. New live wire tests (`api/tests/sender_identity_live.rs`, `#[ignore]`d, against the local rig) drive the real router with a real session against a fake agent gateway that captures the exact payload: for POST /api/chat the identity system row is `messages[0]`, the user turn stays unprefixed, and the name appears nowhere else; for a channel @mention the stamp sits at `messages[1]` after the room prompt with the transcript following. 2/2 live tests pass; automated turns render exactly as before.
