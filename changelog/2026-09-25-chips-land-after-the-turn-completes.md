- **Chips land after the turn completes.** A tool event arriving after the assistant message finished persisting used to drop its chips silently: `speaking_target` matched only streaming turns, so a fast turn — or a tool event delayed behind the talaria-events poster thread's queue — had nothing to land on, and channel turns had no fallback at all. The channel path now falls back to the agent's newest channel message within 15 minutes regardless of status, and the conversation fallback is bounded to the same 15-minute window instead of reaching across the agent's whole history. Chips with no turn in the window land nowhere, dropped on purpose — but the tool-events route now logs the drop (`chips from <tool> for <agent> had no turn to land on — dropped`) instead of vanishing. The built-in soul templates gain the behavior rule that goes with it: a turn that creates something ends with the platform's link chip for it, and a permission ask goes through `expose_tools` or the approval chip, never a prose-only "may I ...?".

  Verified: `bun run gate` green — invariants/docs/gen-docs/changelog/cleanup-sweep clean; fmt + clippy `-D warnings` + cargo tests for `talaria-agent-role-templates`, `talaria-api`, `talaria-chips`, `talaria-routes-fleet` (10 suites, 0 failures). Unit suites 2+8 passed. The relocated live regression suite — `api/tests/it/chips_landing_live.rs`, joined to the consolidated test binary (it was a stray root-level crate after main's one-binary restructure) with the shared support module — ran against the real dev Postgres (rig on 127.0.0.1:55441): `fast_completed_channel_turn_still_gets_its_link_chip`, `stale_fallback_expires`, `approval_chip_lands_with_action_id_rewritten` — 3 passed, 0 failed.
