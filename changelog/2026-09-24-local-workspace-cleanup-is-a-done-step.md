- **Local workspace cleanup is a step of every task, not a reminder after the disk fills.** Agents building this repo were leaving the worktree, devbox, or scratch checkout the task created. The stop gate only blocks once those artifacts are stale or the disk is already over the line, and only Claude Code ran it. Claiming done now requires the local workspace to be gone — the rule loads from `AGENTS.md` for every harness, the procedure is the cleanup skill, and Codex's Stop hook runs the same gate Claude's does (exit 2 continues the turn). Pi, Oh My Pi, and opencode have no main-agent Stop event that can block a turn; a settle hook would also fire for every subagent, so it is not wired, and the rule still binds. The sweep still does not delete a fresh worktree.
  Verified: `bun run gate` (docs and hook wiring only — check, doc links, the cleanup anchors; no surface to exercise).
